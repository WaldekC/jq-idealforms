/*--------------------------------------------------------------------------

  jq-idealforms 2.0

  * Author: Cedric Ruiz
  * License: GPL or MIT
  * Demo: http://elclanrs.github.com/jq-idealforms/
  *

--------------------------------------------------------------------------*/(function($) {
  "use strict";
  $.idealforms = {};
  var Utils = {
    getMaxWidth: function($elms) {
      var maxWidth = 0;
      return $elms.each(function() {
        $(this).outerWidth() > maxWidth && (maxWidth = $(this).outerWidth());
      }), maxWidth;
    },
    getMaxHeight: function($elms) {
      var maxHeight = 0;
      return $elms.each(function() {
        $(this).outerHeight() > maxHeight && (maxHeight = $(this).outerHeight());
      }), maxHeight;
    },
    getLessVar: function(name, prop) {
      var value = $('<p class="' + name + '"></p>').hide().appendTo("body").css(prop);
      return $("." + name).remove(), /^\d+/.test(value) ? parseInt(value, 10) : value;
    },
    getKeys: function(obj) {
      var keys = [];
      for (var key in obj) keys.push(key);
      return keys;
    },
    isFunction: function(obj) {
      return typeof obj == "function";
    },
    isRegex: function(obj) {
      return obj instanceof RegExp;
    },
    getByNameOrId: function(str) {
      if ($('[name="' + str + '"]').length) return $('[name="' + str + '"]');
      if ($('[name="' + str + '"]').length) return $("#" + str);
      $.error('The field "' + str + "\" doesn't exist.");
    },
    getIdealType: function($el) {
      var type = $el.attr("type") || $el[0].tagName.toLowerCase();
      return /(text|password|email|number|search|url|tel)/.test(type) && "text" || /file/.test(type) && "file" || /select/.test(type) && "select" || /(radio|checkbox)/.test(type) && "radiocheck" || /(button|submit|reset)/.test(type) && "button" || /h\d/.test(type) && "description" || /hr/.test(type) && "separator";
    },
    makeInput: function(name, type, list, placeholder) {
      var markup, items = [], i, len;
      /(text|password|email|number|search|url|tel|file)/.test(type) && (markup = '<input type="' + type + '" ' + 'id="' + name + '" ' + 'name="' + name + '" ' + (placeholder && 'placeholder="' + placeholder + '"') + "/>");
      if (/select/.test(type)) {
        items = [];
        for (i = 0, len = list.length; i < len; i++) items.push('<option value="' + list[i] + '">' + list[i] + "</option>");
        markup = '<select id="' + name + '" name="' + name + '">' + items.join("") + "</select>";
      }
      if (/(radio|checkbox)/.test(type)) {
        items = [];
        for (i = 0, len = list.length; i < len; i++) items.push('<label><input type="' + type + '" name="' + name + '" value="' + list[i] + '" />' + list[i] + "</label>");
        markup = items.join("");
      }
      return markup;
    }
  };
  $.fn.tabs = function(ops) {
    var o = $.extend({
      tabContainer: ""
    }, ops), $contents = this, $container = $(o.tabContainer), $wrapper = $('<ul class="ideal-tabs-wrap"/>'), $tabs = function() {
      var tabs = [];
      return $contents.each(function() {
        var $fs = $(this), name = $fs.attr("name"), html = '<li class="ideal-tabs-tab">' + name + '<i class="ideal-tabs-tab-counter ideal-tabs-tab-counter-zero">0</i>' + "</li>";
        tabs.push(html);
      }), $(tabs.join(""));
    }(), maxWidth = 0, Actions = {
      getCurIdx: function() {
        return $tabs.filter(".ideal-tabs-tab-active").index();
      },
      getTabIdxByName: function(name) {
        var re = new RegExp(name, "i"), $tab = $tabs.filter(function() {
          return re.test($(this).text());
        }), idx = $tab.index();
        return idx;
      }
    }, Methods = {
      switchTab: function(ops) {
        var def = $.extend({
          name: "",
          idx: null
        }, ops), idx = def.idx !== null ? def.idx : Actions.getTabIdxByName(def.name);
        $tabs.removeClass("ideal-tabs-tab-active"), $tabs.eq(idx).addClass("ideal-tabs-tab-active"), $contents.eq(idx).is(":visible") || $contents.hide().eq(idx).css("visibility", "hidden").show().fadeTo(0, 0).css("visibility", "visible").fadeTo("normal", 1);
      },
      nextTab: function() {
        var idx = Actions.getCurIdx() + 1;
        idx > $tabs.length - 1 ? Methods.firstTab() : Methods.switchTab({
          idx: idx
        });
      },
      prevTab: function() {
        var idx = Actions.getCurIdx() - 1;
        Methods.switchTab({
          idx: idx
        });
      },
      firstTab: function() {
        Methods.switchTab({
          idx: 0
        });
      },
      lastTab: function() {
        Methods.switchTab({
          idx: $tabs.length - 1
        });
      },
      updateCounter: function(name, text) {
        var idx = Actions.getTabIdxByName(name), $counter = $tabs.eq(idx).find(".ideal-tabs-tab-counter");
        $counter.removeClass("ideal-tabs-tab-counter-zero"), console.log(name + ":" + text), text || $counter.addClass("ideal-tabs-tab-counter-zero"), $counter.attr("title", "Invalid fields").html(text);
      }
    };
    for (var m in Methods) $contents[m] = Methods[m];
    return $tabs.first().addClass("ideal-tabs-tab-active").end().click(function() {
      var name = $(this).text();
      $contents.switchTab({
        name: name
      });
    }), $wrapper.append($tabs).appendTo($container), $tabs.each(function() {
      maxWidth += $(this).outerWidth();
    }), maxWidth > $wrapper.outerWidth() && $wrapper.addClass("ideal-tabs-wrap-full"), $contents.addClass("ideal-tabs-content").each(function() {
      var $this = $(this), name = $(this).attr("name");
      $this.data("ideal-tabs-content-name", name).removeAttr("name");
    }).hide().first().show(), $contents;
  }, $.fn.toCustomSelect = function() {
    return this.each(function() {
      var $select = $(this), Select = function() {
        var $options = $select.find("option"), $newSelect = $('<ul class="ideal-select ' + $select.attr("name") + '"/>'), $menu = $('<li><span class="title">' + $options.filter(":selected").text() + "</span></li>"), items = function() {
          var items = [];
          return $options.each(function() {
            var $this = $(this);
            items.push('<li class="item" ideal-value="' + $this.val() + '">' + $this.text() + "</li>");
          }), items;
        }();
        return $menu.append('<ul class="sub">' + items.join("") + "</ul>"), $newSelect.append($menu), {
          options: $options,
          select: $newSelect,
          title: $menu.find(".title"),
          sub: $menu.find(".sub"),
          items: $menu.find(".sub li")
        };
      }(), Actions = {
        init: function() {
          $select.css({
            position: "absolute",
            left: "-9999px"
          }), Select.select.insertAfter($select), Select.select.css("min-width", Utils.getMaxWidth(Select.items)), Select.sub.hide(), Select.items.eq(Select.options.filter(":selected").index()).addClass("selected");
        }(),
        noWindowScroll: function(e) {
          (e.which === 40 || e.which === 38 || e.which === 13) && e.preventDefault();
        },
        focusHack: function() {
          setTimeout(function() {
            $select.trigger("focus");
          }, 1);
        },
        focus: function() {
          Select.select.addClass("focus"), $(document).on("keydown.noscroll", Actions.noWindowScroll);
        },
        blur: function() {
          Select.select.removeClass("open focus"), $(document).off(".noscroll");
        },
        scrollIntoView: function(dir) {
          var $selected = Select.items.filter(".selected"), itemHeight = $selected.outerHeight(), menuHeight = Select.sub.outerHeight(), isInView = function() {
            var elPos = $selected.position().top + itemHeight;
            return dir === "down" ? elPos <= menuHeight : elPos > 0;
          }();
          isInView || (itemHeight = dir === "down" ? itemHeight : -itemHeight, Select.sub.scrollTop(Select.sub.scrollTop() + itemHeight));
        },
        scrollToItem: function() {
          var idx = Select.items.filter(".selected").index(), height = Select.items.outerHeight(), items = Select.items.length, allHeight = height * items, curHeight = height * (items - idx);
          Select.sub.scrollTop(allHeight - curHeight);
        },
        showMenu: function() {
          Select.sub.fadeIn("fast"), Select.select.addClass("open"), Actions.select(Select.options.filter(":selected").index()), Actions.scrollToItem();
        },
        hideMenu: function() {
          Select.sub.hide(), Select.select.removeClass("open");
        },
        select: function(idx) {
          Select.items.removeClass("selected"), Select.items.eq(idx).addClass("selected");
        },
        change: function(idx) {
          var text = Select.items.eq(idx).text();
          Actions.select(idx), Select.title.text(text), Select.options.eq(idx).prop("selected", !0), $select.trigger("change");
        },
        keydown: function(key) {
          var idx = Select.items.filter(".selected").index(), keys = {
            9: function() {
              Select.select.is(".menu") && (Actions.blur(), Actions.hideMenu());
            },
            13: function() {
              Select.select.is(".menu") && (Select.select.is(".open") ? Actions.hideMenu() : Actions.showMenu()), Actions.change(idx);
            },
            27: function() {
              Select.select.is(".menu") && Actions.hideMenu();
            },
            40: function() {
              idx < Select.options.length - 1 && (Select.select.is(".open") ? Actions.select(idx + 1) : Actions.change(idx + 1)), Actions.scrollIntoView("down");
            },
            38: function() {
              idx > 0 && (Select.select.is(".open") ? Actions.select(idx - 1) : Actions.change(idx - 1)), Actions.scrollIntoView("up");
            },
            "default": function() {
              var letter = String.fromCharCode(key), selIdx = Select.items.filter(".selected").parent().index(), curIdx = Select.items.filter(function() {
                var re = new RegExp("^" + letter, "i");
                return re.test($(this).text());
              }).first().index();
              Actions.change(~curIdx ? curIdx : selIdx), Actions.scrollToItem(), Actions.focusHack();
            }
          };
          keys[key] ? keys[key]() : keys["default"]();
        }
      }, events = {
        focus: Actions.focus,
        "blur.menu": function() {
          Actions.blur(), Actions.hideMenu();
        },
        "blur.list": function() {
          Actions.blur();
        },
        keydown: function(e) {
          Actions.keydown(e.which);
        },
        "clickItem.menu": function() {
          Actions.change($(this).index()), Actions.hideMenu();
        },
        "clickItem.list": function() {
          Actions.change($(this).index());
        },
        "clickTitle.menu": function() {
          Actions.focus(), Actions.showMenu(), $select.trigger("focus");
        },
        "hideOutside.menu": function() {
          $select.off("blur.menu"), $(document).on("mousedown.ideal", function(evt) {
            $(evt.target).closest(Select.select).length ? Actions.focusHack() : ($(document).off("mousedown.ideal"), $select.on("blur.menu", events["blur.menu"]));
          });
        },
        "mousedown.list": function() {
          Actions.focusHack();
        }
      }, disableEvents = function() {
        Select.select.removeClass("menu list"), $select.off(".menu .list"), Select.items.off(".menu .list"), Select.select.off(".menu .list"), Select.title.off(".menu .list");
      };
      Select.select.on("menu", function() {
        disableEvents(), Select.select.addClass("menu"), Actions.hideMenu(), $select.on({
          "blur.menu": events["blur.menu"],
          "focus.menu": events.focus,
          "keydown.menu": events.keydown
        }), Select.select.on("mousedown.menu", events["hideOutside.menu"]), Select.items.on("click.menu", events["clickItem.menu"]), Select.title.on("click.menu", events["clickTitle.menu"]);
      }), Select.select.on("list", function() {
        disableEvents(), Select.select.addClass("list"), Actions.showMenu(), $select.on({
          "blur.list": events["blur.list"],
          "focus.list": events.focus,
          "keydown.list": events.keydown
        }), Select.select.on("mousedown.list", events["mousedown.list"]), Select.items.on("mousedown.list", events["clickItem.list"]);
      }), Select.select.on("reset", function() {
        Actions.change(0);
      }), Select.select.trigger("menu");
    });
  }, $.fn.toCustomRadioCheck = function() {
    return this.each(function() {
      var $this = $(this), $span = $("<span/>");
      $this.is(":checkbox") ? $span.addClass("ideal-check") : $span.addClass("ideal-radio"), $this.is(":checked") && $span.addClass("checked"), $span.insertAfter($this), $(this).parent("label").attr("onclick", ""), $this.css({
        position: "absolute",
        left: "-9999px"
      }).on({
        change: function() {
          var $this = $(this);
          $this.trigger("focus"), $this.is(":radio") ? ($this.parent().siblings("label").children(".ideal-radio").removeClass("checked"), $this.is(":checked") && $this.next(".ideal-radio").addClass("checked")) : $this.is(":checked") ? $span.addClass("checked") : $span.removeClass("checked");
        },
        focus: function() {
          $span.parent().addClass("focus");
        },
        blur: function() {
          $span.parent().removeClass("focus");
        },
        click: function() {
          $(this).trigger("focus");
        }
      });
    });
  }, $.fn.toCustomFile = function() {
    return this.each(function() {
      var $file = $(this), $wrap = $('<div class="ideal-file-wrap">'), $input = $('<input type="text" class="ideal-file-filename" />'), $button = $('<button type="button" class="ideal-file-upload">Open</button>');
      $file.css({
        position: "absolute",
        left: "-9999px"
      }), $button.attr("tabIndex", -1).click(function() {
        $file.trigger("click");
      }), $file.attr("tabIndex", -1).on({
        change: function() {
          var multipleSupport = typeof $("input")[0].multiple != "undefined", files = [], fileArr, filename;
          if (multipleSupport) {
            fileArr = $file[0].files;
            for (var i = 0, len = fileArr.length; i < len; i++) files.push(fileArr[i].name);
            filename = files.join(", ");
          } else filename = $file.val().split("\\").pop();
          $input.val(filename), $input.attr("title", filename);
        },
        focus: function() {
          $input.trigger("focus");
        }
      }), $input.on({
        keyup: function() {
          $file.trigger("change");
        },
        focus: function() {
          $file.trigger("change");
        },
        blur: function() {
          $file.trigger("blur");
        },
        keydown: function(e) {
          e.which === 13 && $file.trigger("click"), (e.which === 8 || e.which === 46) && $file.val("");
        }
      }), $wrap.append($button, $input).insertAfter($file);
    });
  }, $.idealforms.errors = {
    required: "This field is required.",
    number: "Must be a number.",
    digits: "Must be only digits.",
    name: "Must be at least 3 characters long, and must only contain letters.",
    username: "Must be at between 4 and 32 characters long and start with a letter. You may use letters, numbers, underscores, and one dot (.)",
    pass: "Must be at least 6 characters long, and contain at least one number, one uppercase and one lowercase letter.",
    strongpass: "Must be at least 8 characters long and contain at least one uppercase and one lowercase letter and one number or special character.",
    email: "Must be a valid e-mail address. <em>(e.g. user@gmail.com)</em>",
    phone: "Must be a valid US phone number. <em>(e.g. 555-123-4567)</em>",
    zip: "Must be a valid US zip code. <em>(e.g. 33245 or 33245-0003)</em>",
    url: "Must be a valid URL. <em>(e.g. www.google.com)</em>",
    min: "Must be at least <strong>{0}</strong> characters long.",
    minOption: "Check at least <strong>{0}</strong> options.",
    max: "No more than <strong>{0}</strong> characters long.",
    maxOption: "No more than <strong>{0}</strong> options allowed.",
    date: "Must be a valid date. <em>(e.g. {0})</em>",
    exclude: '"{0}" is not available.',
    excludeOption: "{0}",
    equalto: 'Must be the same value as <strong>"{0}"</strong>',
    extension: 'File(s) must have a valid extension. <em>(e.g. "{0}")</em>'
  };
  var getFilters = function() {
    return {
      required: {
        error: $.idealforms.errors.required
      },
      number: {
        regex: /\d+/,
        error: $.idealforms.errors.number
      },
      digits: {
        regex: /^\d+$/,
        error: $.idealforms.errors.digits
      },
      name: {
        regex: /^[A-Za-z]{3,}$/,
        error: $.idealforms.errors.name
      },
      username: {
        regex: /^[a-z](?=[\w.]{3,31}$)\w*\.?\w*$/i,
        error: $.idealforms.errors.username
      },
      pass: {
        regex: /(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{6,}/,
        error: $.idealforms.errors.pass
      },
      strongpass: {
        regex: /(?=^.{8,}$)((?=.*\d)|(?=.*\W+))(?![.\n])(?=.*[A-Z])(?=.*[a-z]).*$/,
        error: $.idealforms.errors.strongpass
      },
      email: {
        regex: /[^@]+@[^@]/,
        error: $.idealforms.errors.email
      },
      phone: {
        regex: /^[2-9]\d{2}-\d{3}-\d{4}$/,
        error: $.idealforms.errors.phone
      },
      zip: {
        regex: /^\d{5}$|^\d{5}-\d{4}$/,
        error: $.idealforms.errors.zip
      },
      url: {
        regex: /^(?:(ftp|http|https):\/\/)?(?:[\w\-]+\.)+[a-z]{3,6}$/i,
        error: $.idealforms.errors.url
      },
      min: {
        regex: function(input, value) {
          var i = input.input, min = input.userOptions.data.min;
          return i.is('[type="checkbox"], [type="radio"], select') ? (this.error = $.idealforms.errors.minOption.replace("{0}", min), i.filter(":checked").length >= min) : (this.error = $.idealforms.errors.min.replace("{0}", min), value.length >= min);
        }
      },
      max: {
        regex: function(input, value) {
          var i = input.input, max = input.userOptions.data.max;
          return i.is('[type="checkbox"], [type="radio"], select') ? (this.error = $.idealforms.errors.maxOption.replace("{0}", max), i.filter(":checked").length <= max) : (this.error = $.idealforms.errors.max.replace("{0}", max), value.length <= max);
        }
      },
      date: {
        regex: function(input, value) {
          var userFormat = input.userOptions.data && input.userOptions.data.date ? input.userOptions.data.date : "mm/dd/yyyy", delimiter = /[^mdy]/.exec(userFormat)[0], theFormat = userFormat.split(delimiter), theDate = value.split(delimiter), isDate = function(date, format) {
            var m, d, y;
            for (var i = 0, len = format.length; i < len; i++) /m/.test(format[i]) && (m = date[i]), /d/.test(format[i]) && (d = date[i]), /y/.test(format[i]) && (y = date[i]);
            return m > 0 && m < 13 && y && y.length === 4 && d > 0 && d <= (new Date(y, m, 0)).getDate();
          };
          return this.error = $.idealforms.errors.date.replace("{0}", userFormat), isDate(theDate, theFormat);
        }
      },
      exclude: {
        regex: function(input, value) {
          var i = input.input;
          return i.is('[type="checkbox"], [type="radio"], select') ? this.error = $.idealforms.errors.excludeOption.replace("{0}", value) : $.idealforms.errors.exclude.replace("{0}", value), !~$.inArray(value, input.userOptions.data.exclude);
        }
      },
      equalto: {
        regex: function(input, value) {
          var $equals = $(input.userOptions.data.equalto), $input = input.input, name = $equals.attr("name") || $equals.attr("id"), isValid = $equals.parents(".ideal-field").filter(function() {
            return $(this).data("isValid") === !0;
          }).length;
          return isValid ? (this.error = $.idealforms.errors.equalto.replace("{0}", name), $input.val() === $equals.val()) : !1;
        }
      },
      extension: {
        regex: function(input, value) {
          var files = input.input[0].files || [ {
            name: value
          } ], extensions = input.userOptions.data.extension, re = new RegExp("\\." + extensions.join("|") + "$", "i"), valid = !1;
          for (var i = 0, len = files.length; i < len; i++) valid = re.test(files[i].name) ? !0 : !1;
          return this.error = $.idealforms.errors.extension.replace("{0}", extensions.join('", "')), valid;
        }
      }
    };
  }, Flags = {
    noerror: function(i) {
      i.parent().siblings(".error").hide();
    },
    noicons: function(i) {
      i.siblings(".valid-icon, .invalid-icon").hide();
    },
    novalidicon: function(i) {
      i.siblings(".valid-icon").hide();
    },
    noinvalidicon: function(i) {
      i.siblings(".invalid-icon").hide();
    },
    noclass: function(i) {
      i.parents(".ideal-field").removeClass("valid invalid");
    },
    novalidclass: function(i) {
      i.parents(".ideal-field").removeClass("valid");
    },
    noinvalidclass: function(i) {
      i.parents(".ideal-field").removeClass("invalid");
    }
  };
  $.fn.idealforms = function(ops) {
    var o = $.extend({
      inputs: {},
      customFilters: {},
      customFlags: {},
      globalFlags: "",
      onSuccess: function(e) {
        alert("Thank you...");
      },
      onFail: function() {
        alert("The form does not validate! Check again...");
      },
      responsiveAt: "auto",
      customInputs: !0
    }, ops), $form = this, $idealTabs = function() {
      var $t = !1, $tabs = $form.find("section");
      return $tabs.length && ($form.prepend('<div class="ideal-wrap ideal-tabs"/>'), $t = $tabs.tabs({
        tabContainer: ".ideal-tabs"
      }), $form.find(".ideal-tabs-wrap").addClass("ideal-full-width")), $t;
    }(), Filters = getFilters(), getFormInputs = function() {
      return {
        inputs: $form.find("input, select, textarea, :button"),
        labels: $form.find("div > label:first-child"),
        text: $form.find('input:not([type="checkbox"], [type="radio"]), textarea'),
        select: $form.find("select"),
        radiocheck: $form.find('input[type="radio"], input[type="checkbox"]'),
        buttons: $form.find(":button"),
        file: $form.find('input[type="file"]'),
        headings: $form.find("h1, h2, h3, h4, h5, h6, p"),
        separators: $form.find("hr")
      };
    }, getUserInputs = function() {
      return $('[name="' + Utils.getKeys(o.inputs).join('"], [name="') + '"],' + "." + Utils.getKeys(Filters).join(", ."));
    }, LessVars = {
      fieldWidth: Utils.getLessVar("ideal-field-width", "width")
    }, Actions = {
      getCurrentTab: function($input) {
        var $tabContent = $input.parents(".ideal-tabs-content"), tabName = $tabContent.data("ideal-tabs-content-name"), $fields = $($tabContent.find(".ideal-field").parents(".ideal-wrap").get().reverse());
        return {
          content: $tabContent,
          name: tabName,
          fields: $fields
        };
      },
      doMarkup: function($el) {
        var type = Utils.getIdealType($el), addValidationEls = function() {
          var $error = $('<span class="error" />'), $valid = $('<i class="icon valid-icon" />'), $invalid = $("<i/>", {
            "class": "icon invalid-icon",
            click: function() {
              var $this = $(this);
              $this.siblings("label").length ? $this.siblings("label:first").find("input").focus() : $this.siblings("input, select, textarea").focus();
            }
          });
          $el.parents(".ideal-field").append($valid.add($invalid).hide()).after($error.hide());
        }, idealTypes = {
          "default": $.noop,
          defaultInput: function() {
            $el.wrapAll('<span class="ideal-field"/>'), addValidationEls();
          },
          button: function() {
            o.customInputs && $el.addClass("ideal-button");
          },
          file: function() {
            idealTypes.defaultInput(), o.customInputs && $el.toCustomFile();
          },
          select: function() {
            idealTypes.defaultInput(), o.customInputs && $el.toCustomSelect();
          },
          text: function() {
            idealTypes.defaultInput();
          },
          radiocheck: function() {
            var isWrapped = $el.parents(".ideal-field").length, $all = $el.parent().siblings("label:not(:first)").andSelf();
            o.customInputs && $el.toCustomRadioCheck();
            if (!!isWrapped) return !1;
            $all.wrapAll('<span class="ideal-field ideal-radiocheck"/>'), addValidationEls();
          },
          description: function() {
            var isWrapped = $el.parents(".ideal-field").length, $all = $el.siblings().andSelf();
            isWrapped || $all.wrapAll('<span class="ideal-heading ideal-full-width"/>');
          },
          separator: function() {
            $el.wrapAll('<div class="ideal-separator ideal-full-width"/>');
          }
        };
        $el.closest("div").addClass("ideal-wrap"), idealTypes[type] ? idealTypes[type]() : idealTypes["default"]();
      },
      adjust: function() {
        var formInputs = getFormInputs(), userInputs = getUserInputs();
        formInputs.inputs.attr("autocomplete", "off"), $('[name="' + Utils.getKeys(o.inputs).join('"], [name="') + '"]').each(function() {
          this.className = o.inputs[this.name].filters;
        }), $form.find(".ideal-full-width").width($form.width()), formInputs.labels.addClass("ideal-label").width(Utils.getMaxWidth(formInputs.labels)), $idealTabs.length ? $idealTabs.each(function() {
          $(this).find(".ideal-heading:first").addClass("first-child");
        }) : $form.find(".ideal-heading:first").addClass("first-child"), jQuery.ui && $form.find("input.date").each(function() {
          var userInput = o.inputs[this.name], data = userInput.data && userInput.data.date, format = data ? data.replace("yyyy", "yy") : "mm/dd/yy";
          $(this).datepicker({
            dateFormat: format,
            beforeShow: function(i) {
              $(i).addClass("open");
            },
            onChangeMonthYear: function() {
              var w = $(this).outerWidth();
              setTimeout(function() {
                $(this).datepicker("widget").width(w);
              }, 1);
            },
            onClose: function() {
              $(this).removeClass("open");
            }
          });
        }).on("focus keyup", function() {
          var t = $(this), w = t.outerWidth();
          t.datepicker("widget").width(w);
        }).parent().siblings(".error").addClass("hidden"), "placeholder" in $("<input/>")[0] || formInputs.text.each(function() {
          $(this).val($(this).attr("placeholder"));
        }).on({
          focus: function() {
            this.value === $(this).attr("placeholder") && $(this).val("");
          },
          blur: function() {
            $(this).val() || $(this).val($(this).attr("placeholder"));
          }
        });
      },
      init: function() {
        var formInputs = getFormInputs();
        $form.css("visibility", "visible").addClass("ideal-form"), $form.attr("novalidate", "novalidate"), jQuery.ui && ($.datepicker._checkOffset = function(a, b, c) {
          return b;
        }), formInputs.inputs.add(formInputs.headings).add(formInputs.separators).each(function() {
          Actions.doMarkup($(this));
        }), Actions.adjust();
      },
      validate: function(input, value) {
        var isValid = !0, error = "", $input = input.input, userOptions = input.userOptions, userFilters = userOptions.filters;
        if (userFilters) {
          !value && /required/.test(userFilters) && (error = userOptions.errors && userOptions.errors.required ? userOptions.errors.required : Filters.required.error, isValid = !1);
          if (value) {
            userFilters = userFilters.split(/\s/);
            for (var i = 0, len = userFilters.length; i < len; i++) {
              var uf = userFilters[i], theFilter = Filters[uf] || {};
              if (theFilter && (Utils.isFunction(theFilter.regex) && !theFilter.regex(input, value) || Utils.isRegex(theFilter.regex) && !theFilter.regex.test(value))) {
                isValid = !1, error = userOptions.errors && userOptions.errors[uf] || theFilter.error;
                break;
              }
            }
          }
        }
        return {
          isValid: isValid,
          error: error
        };
      },
      analyze: function(input, evt) {
        var isRadiocheck = input.is('[type="checkbox"], [type="radio"]'), $input = function() {
          var userInputs = getUserInputs();
          return isRadiocheck ? userInputs.filter('[name="' + input.attr("name") + '"]') : userInputs.filter(input);
        }(), currentTabName = Actions.getCurrentTab($input).name, userOptions = o.inputs[input.attr("name")] || {
          filters: input.attr("class")
        }, value = function() {
          var iVal = input.val();
          if (iVal === input.attr("placeholder")) return;
          return isRadiocheck ? userOptions && " " : iVal;
        }(), $field = input.parents(".ideal-field"), $error = $field.next(".error"), $invalid = function() {
          return isRadiocheck ? input.parent().siblings(".invalid-icon") : input.siblings(".invalid-icon");
        }(), $valid = function() {
          return isRadiocheck ? input.parent().siblings(".valid-icon") : input.siblings(".valid-icon");
        }(), test = Actions.validate({
          input: $input,
          userOptions: userOptions
        }, value), flags = function() {
          var f = userOptions.flags ? userOptions.flags : "";
          return o.globalFlags && (f += o.globalFlags), f.split(/\s/);
        }(), doFlags = function() {
          for (var i = 0, len = flags.length, f; i < len; i++) {
            f = flags[i];
            if (!Flags[f]) break;
            Flags[f]($input, evt);
          }
        };
        $field.removeClass("valid invalid").data("isValid", !0), $error.add($invalid).add($valid).hide(), value && test.isValid && ($error.add($invalid).hide(), $field.addClass("valid").data("isValid", !0), $valid.show()), test.isValid || ($invalid.show(), $field.addClass("invalid").data("isValid", !1), $form.find(".error").hide(), evt !== "blur" && $error.html(test.error).show()), $idealTabs.length && $idealTabs.updateCounter(currentTabName, $form.getInvalid(currentTabName).length), doFlags();
      },
      attachEvents: function() {
        getUserInputs().on("keyup change focus blur", function(e) {
          Actions.analyze($(this), e.type);
        });
      },
      responsive: function() {
        var formInputs = getFormInputs(), maxWidth = LessVars.fieldWidth + formInputs.labels.outerWidth(), $emptyLabel = formInputs.labels.filter(function() {
          return $(this).html() === "&nbsp;";
        }), $customSelect = $form.find(".ideal-select"), $datePicker = $form.find("input.hasDatepicker");
        o.responsiveAt === "auto" ? $form.width() < maxWidth ? $form.addClass("stack") : $form.removeClass("stack") : $(window).width() < o.responsiveAt ? $form.addClass("stack") : $form.removeClass("stack"), $form.is(".stack") ? ($emptyLabel.hide(), $customSelect.trigger("list")) : ($emptyLabel.show(), $customSelect.trigger("menu")), $form.find(".ideal-full-width").width($form.width()), $datePicker.length && $datePicker.datepicker("hide");
      }
    }, PublicMethods = {
      addFields: function(fields) {
        fields = fields.reverse();
        var add = function(ops) {
          var addAfterOrBefore = ops.addAfter && $(Utils.getByNameOrId(ops.addAfter)).parents(".ideal-wrap") || ops.addBefore && $(Utils.getByNameOrId(ops.addBefore)).parents(".ideal-wrap") || $form.find(".ideal-wrap").last(), name = ops.name, userOptions = {
            filters: ops.filters || "",
            data: ops.data || {},
            errors: ops.errors || {},
            flags: ops.flags || ""
          }, title = ops.title, type = ops.type, list = ops.list || "", placeholder = ops.placeholder || "", $field = $("<div><label>" + title + ":</label>" + Utils.makeInput(name, type, list, placeholder) + "</div>"), $input = $field.find("input, select, textarea, :button");
          userOptions.filters && (o.inputs[name] = userOptions), Actions.doMarkup($input), ops.addAfter ? $field.insertAfter(addAfterOrBefore) : ops.addBefore ? $field.insertBefore(addAfterOrBefore) : $field.insertAfter(addAfterOrBefore);
        };
        for (var i = 0, len = fields.length; i < len; i++) add(fields[i]);
        return $form.reload(), $form;
      },
      removeFields: function(fields) {
        var f = [];
        for (var i = 0, l = fields.length; i < l; i++) f.push(Utils.getByNameOrId(fields[i]).get(0));
        return $(f).parents(".ideal-wrap").remove(), $form;
      },
      getInvalid: function(tabName) {
        return tabName && $idealTabs.length ? $idealTabs.filter(function() {
          var re = new RegExp(tabName, "i");
          return re.test($(this).data("ideal-tabs-content-name"));
        }).find(".ideal-field").filter(function() {
          return $(this).data("isValid") === !1;
        }) : $form.find(".ideal-field").filter(function() {
          return $(this).data("isValid") === !1;
        });
      },
      isValid: function() {
        return !$form.getInvalid().length;
      },
      isValidField: function(str) {
        var $input = Utils.getByNameOrId(str);
        return $input.parents(".ideal-field").data("isValid") === !0;
      },
      focusFirst: function() {
        return $idealTabs.length ? $idealTabs.filter(":visible").find("input:first").focus() : $form.find("input:first").focus(), $form;
      },
      focusFirstInvalid: function() {
        var $first = $form.getInvalid().first(), tabName = $first.parents(".ideal-tabs-content").data("ideal-tabs-content-name");
        return $idealTabs.length && $idealTabs.switchTab({
          name: tabName
        }), $first.find("input:first").focus(), $form;
      },
      switchTab: function(name, idx) {
        return $idealTabs.switchTab({
          name: name,
          idx: idx
        }), $form;
      },
      nextTab: function() {
        return $idealTabs.nextTab(), $form;
      },
      prevTab: function() {
        return $idealTabs.prevTab(), $form;
      },
      firstTab: function() {
        return $idealTabs.firstTab(), $form;
      },
      lastTab: function() {
        return $idealTabs.lastTab(), $form;
      },
      fresh: function() {
        var userInputs = getUserInputs();
        return userInputs.blur().parents(".ideal-field").removeClass("valid invalid"), $idealTabs.length && $idealTabs.firstTab(), $form;
      },
      reload: function() {
        Actions.adjust(), Actions.attachEvents();
      },
      reset: function() {
        var formInputs = getFormInputs();
        return formInputs.text.val(""), formInputs.radiocheck.removeAttr("checked"), formInputs.select.find("option").first().prop("selected", !0), $form.find(".ideal-select").trigger("reset"), formInputs.inputs.change().blur(), $form.focusFirst(), $form;
      },
      submitData: function() {
        return $form.submit(), $form;
      }
    };
    for (var m in PublicMethods) $form[m] = PublicMethods[m];
    return $form.on({
      keydown: function(e) {
        e.which === 13 && e.preventDefault();
      },
      submit: function(e) {
        $form.isValid() ? o.onSuccess(e) : (e.preventDefault(), o.onFail(), $form.focusFirstInvalid());
      }
    }), o.responsiveAt && ($(window).resize(Actions.responsive), Actions.responsive()), $.extend(!0, Filters, o.customFilters), $.extend(!0, Flags, o.customFlags), Actions.init(), Actions.attachEvents(), $form.fresh(), this;
  };
})(jQuery);